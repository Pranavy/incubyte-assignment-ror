<% content_for :title, "Salary metrics by job title — Incubyte Assignment" %>

<div class="container py-4">
  <h1 class="h3 mb-2">Salary metrics by job title</h1>
  <p class="text-muted mb-4">
    <%= link_to "Salary metrics by country", salary_metrics_country_path, class: "text-decoration-none" %>
  </p>

  <p class="text-muted mb-4">
    <strong>Gross</strong> figures use stored employee salaries.
    <strong>Net</strong> uses TDS rules effective on the selected date, with defaults of 10% (India), 12% (United States), and 0% for other countries when no rule applies.
  </p>

  <div class="card shadow-sm border mb-4">
    <div class="card-body">
      <%= form_with url: salary_metrics_job_title_path, method: :get, local: true, class: "row g-3 align-items-end" do %>
        <div class="col-md-6">
          <%= label_tag :job_title_id, "Job title", class: "form-label" %>
          <%= select_tag :job_title_id,
                options_from_collection_for_select(@job_titles, :id, :title, @job_title_id),
                { include_blank: "Select a job title…", class: "form-select", required: true } %>
        </div>
        <div class="col-md-4">
          <%= label_tag :as_of, "As of (for TDS rules)", class: "form-label" %>
          <%= date_field_tag :as_of, @as_of, class: "form-control" %>
        </div>
        <div class="col-md-2">
          <%= submit_tag "Show averages", name: nil, class: "btn btn-primary w-100" %>
        </div>
      <% end %>
    </div>
  </div>

  <% if @job_title_id.present? && @metrics_job_title.nil? %>
    <div class="alert alert-warning" role="alert">
      No job title found for that selection.
    </div>
  <% elsif @metrics_job_title.present? %>
    <div class="card shadow-sm border">
      <div class="card-header bg-light">
        <strong><%= @metrics_job_title[:job_title] %></strong>
        <span class="text-muted small ms-2">(ID <%= @metrics_job_title[:job_title_id] %>)</span>
      </div>
      <div class="card-body">
        <% if @metrics_job_title[:avg_gross_salary].nil? %>
          <p class="text-muted mb-0">No employees have this job title. Averages are not available.</p>
        <% else %>
          <dl class="row mb-0">
            <dt class="col-sm-4 col-md-3">Average gross salary</dt>
            <dd class="col-sm-8 col-md-9"><%= number_to_currency(@metrics_job_title[:avg_gross_salary]) %></dd>
            <dt class="col-sm-4 col-md-3">Average net salary</dt>
            <dd class="col-sm-8 col-md-9"><%= number_to_currency(@metrics_job_title[:avg_net_salary]) %></dd>
            <dt class="col-sm-4 col-md-3">TDS reference date</dt>
            <dd class="col-sm-8 col-md-9"><%= l(@as_of) %></dd>
          </dl>
        <% end %>
      </div>
      <div class="card-footer small text-muted">
        JSON:
        <code class="user-select-all"><%= api_salary_metrics_by_job_title_path(job_title_id: @metrics_job_title[:job_title_id], as_of: @as_of) %></code>
      </div>
    </div>
  <% end %>
</div>
